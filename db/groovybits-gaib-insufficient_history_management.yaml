app: gaib
repo: https://github.com/groovybits/gaib
commit: 887e8edf7f6ce112af4790ac5f45c629b05e3225
defect_id: groovybits-gaib-insufficient_history_management-/
type: insufficient history management
case: /
consequence:
- IC
locations:
- pages/api/chat.ts
- scripts/twitchChat.ts
- config/personalityPrompts.ts
trigger_tests:
- '1. Set up GPT4 PDF Chatbot LangChain: Ensure the project is correctly set up in
  your local environment.

  2. Upload a PDF document for querying.

  3. Run the Chatbot: Execute `python app.py` or the relevant command to start the
  server.

  4. Open the chat interface in the web UI.

  5. Enter a query and observe the response.

  6. Enter a follow-up query that relies on the context of the previous interaction.

  7. Observe if the response demonstrates awareness of the previous interactions or
  if it treats each query as an isolated interaction.(similar to "AttributeError:
  ''NoneType'' object has no attribute ''extract_text''")'
